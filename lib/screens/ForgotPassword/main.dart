import 'package:flutter/material.dart';
import 'ForgotPassword.dart';


class QuenMatKhauApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: ForgotPassword(),
      // Other code if needed for routing, etc.
    );
  }
}
